import styled from "styled-components";

function App() {
  return StyledApp;
}

const StyledApp = styled.div`
  display: flex;
  justify-content: center;
  align-items: center;
`;

export default App;
